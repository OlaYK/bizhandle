version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ibos
    ports:
      - "5432:5432"
    volumes:
      - ibos_pg:/var/lib/postgresql/data

volumes:
  ibos_pg:
